---
import Layout from "../../layouts/Layout.astro";
import "../../css/page.css";
---

<Layout title="Contents | しんぞ" showSidebar={true}>
  <section class="page">
    <div class="container">
      <p class="eyebrow">CONTENTS</p>
      <h1>書いたものとか</h1>
      <p id="message"></p>
      
    </div>
  </section>
  <script>
    const message = document.querySelector("#message");

    const updateMessage = () => {
      if (message) {
        message.textContent =
          window.innerWidth >= 1199
            ? "右側のサイドバーからえらんでね"
            : "上のハンバーガーからえらんでね";
      }
    };

    updateMessage();

    window.addEventListener("resize", updateMessage);
  </script>
</Layout>
